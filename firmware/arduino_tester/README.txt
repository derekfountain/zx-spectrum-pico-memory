Simulator, runs on an Arduino, outputs RAS, CAS and WR waveforms
in the same pattern as the Spectrum ULA. Only much slower.

Pattern is 4 reads in page mode, pause, 1 refresh, 1 read in cycle mode,
1 write in cycle mode.